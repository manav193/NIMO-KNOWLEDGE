# Windows Knowledge Domain

This directory contains Windows-specific execution playbooks, PowerShell guidelines, path-handling rules, and OS-level quirks.

## Contents
- PowerShell quote and escape conventions
- Windows path format handling (`\\` vs `/`)
- Command-line length limitations and workaround patterns
- Windows process management and signal handling
